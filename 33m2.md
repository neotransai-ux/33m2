# 33m2 오피스텔 크롤러 + 예약 분석 웹앱

`web.33m2.co.kr` 단기임대 매물을 수집하고, 로그인 후 매물별 월간 예약 캘린더를 분석하여 적정 월세를 시뮬레이션하는 Flask 기반 웹 애플리케이션.

---

## 📁 프로젝트 구조

```
samsam2/
├── samsam2.py              # Flask 백엔드 (API + 크롤러 + 로그인 + 분석)
├── templates/
│   └── index.html          # 단일 페이지 UI (HTML + CSS + JS)
├── static/                 # (정적 파일, 현재 비어있음)
├── __pycache__/
└── 33m2.md                 # 본 문서
```

---

## 🛠 의존성

| 패키지 | 용도 |
|---|---|
| `flask` | 웹 서버 / 라우팅 / 세션 |
| `requests` | 33m2 REST API 호출 |
| `selenium` | 33m2 로그인 (CSRF/JS 필요) |
| `webdriver-manager` | ChromeDriver 자동 관리 |

설치:
```bash
pip install flask requests selenium webdriver-manager
```

실행:
```bash
python samsam2.py
# http://127.0.0.1:5000
```

---

## 🌐 외부 API 엔드포인트 (33m2)

| 용도 | URL |
|---|---|
| 매물 목록 (정렬/키워드) | `GET https://web.33m2.co.kr/v1/use-auth/rooms` |
| 매물 목록 (좌표 박스) | `GET https://web.33m2.co.kr/v1/use-auth/map/rooms` |
| 매물 예약 캘린더 | `GET https://web.33m2.co.kr/v1/use-auth/rooms/{rid}/schedules?year=&month=` |
| 이미지 CDN | `https://d1pviohoskiraj.cloudfront.net/{picMain}?b=samsamm2&d=640x427` |
| 로그인 | `https://web.33m2.co.kr/sign-in` (Selenium) |

요청 헤더:
- `User-Agent`: Chrome 124 (Win64)
- `Referer`, `Origin`: `https://web.33m2.co.kr`

---

## 🔧 백엔드 (`samsam2.py`)

### 핵심 함수

| 함수 | 설명 |
|---|---|
| `_make_driver(headless)` | Selenium Chrome 드라이버 생성 (`navigator.webdriver` 위장 포함) |
| `_zoom_to_bbox(lat, lng, zoom)` | 중심 좌표 + zoomLevel → 바운딩 박스(sw/ne) 계산 (`360 / 2^zoom * 6` 폭) |
| `fetch_page(sort, propertyTypes, page, size, keyword)` | 일반 rooms API 호출 |
| `fetch_map_page(lat, lng, zoom, sort, ...)` | map/rooms API (좌표 박스 검색) |
| `parse_room(item)` | 원본 응답 → UI용 dict 정규화 (제목/주소/가격/평수/이미지 등) |
| `selenium_login(email, password)` | 33m2 로그인 후 쿠키 dict 반환 |
| `fetch_schedules(rid, year, month, cookies, include_disable)` | 월간 예약 캘린더 조회 → blocked/available 일수 계산 |
| `analyze_room_bookings(rid, cookies, until_month, until_year, include_disable)` | 현재 월부터 종료 월까지 매월 schedule API 순차 호출 |

### 예약 status 분류 상수

```python
_BOOKING_STATUSES = {"BOOKING", "BOOKED", "RESERVED", "UNAVAILABLE", "CLOSED"}
_DISABLE_STATUSES = {"DISABLE", "DISABLED", "BLOCK", "BLOCKED"}
```

- `booking` → 예약된 날 (수익 발생)
- `disable` → 호스트가 막아둔 날 (옵션에 따라 blocked 포함 여부 결정)
- 빈 값 → booking으로 간주

### 응답 구조 다중 파싱

`fetch_schedules`는 다음 3가지 응답 구조를 모두 지원:
- 구조 A: `{"data": [{"date": "...", "status": "..."}]}`
- 구조 B: `{"data": {"schedules": [...]}}`
- 구조 C: `{"data": {"2026-06-01": "booking"}}`

### Flask 라우트

| 메서드 | 경로 | 설명 |
|---|---|---|
| GET | `/` | 메인 페이지 (`index.html`) |
| GET | `/api/crawl` | 매물 크롤링 (`searchMode`: `sort` / `keyword` / `coord`) |
| POST | `/api/login` | 33m2 로그인 (Selenium → 세션에 쿠키 저장) |
| POST | `/api/logout` | 세션 클리어 |
| POST | `/api/booking-analysis` | 선택 매물들에 대해 월별 예약율 분석 |
| GET | `/api/debug-schedule/<rid>` | 단일 매물/월 raw 응답 확인 (디버그용) |

#### `/api/crawl` 쿼리 파라미터

| 파라미터 | 기본값 | 설명 |
|---|---|---|
| `sort` | `POPULAR` | `POPULAR` / `RECENT` / `PRICE_ASC` / `PRICE_DESC` |
| `propertyTypes` | `OFFICETEL` | `OFFICETEL` / `ONE_ROOM` / `TWO_ROOM` / `APARTMENT` |
| `maxPages` | `5` | 페이지당 20개씩 최대 N페이지 |
| `searchMode` | `sort` | `sort` / `keyword` / `coord` |
| `keyword` | - | `searchMode=keyword` 시 검색어 |
| `lat`, `lng`, `zoom` | - | `searchMode=coord` 시 좌표 |

#### `/api/booking-analysis` 요청 바디

```json
{
  "rooms": [{ "rid": 102555, "title": "..." }, ...],
  "until_month": 12,
  "until_year": 2026,
  "include_disable": false
}
```

응답 예:
```json
{
  "success": true,
  "results": [{
    "rid": 102555,
    "title": "햇살가득 풀옵션오피스텔",
    "monthly": {
      "2026-05": { "total": 31, "blocked": 31, "rate": 100, ... }
    },
    "total_days": 245,
    "total_blocked": 198,
    "overall_rate": 80.8,
    "using_fee": 280000,
    "mgmt_fee": 100000
  }]
}
```

---

## 🎨 프론트엔드 (`templates/index.html`)

단일 HTML 파일에 CSS와 JS를 인라인으로 포함. 외부 라이브러리 의존성 없음 (vanilla JS).

### 탭 구조

#### 📋 탭 1: 매물 크롤링

- **검색 방식 토글**: 정렬순 / 키워드 / 좌표 링크
- 좌표 링크는 33m2 검색 결과 URL을 그대로 붙여넣으면 `lat/lng/zoomLevel` 쿼리 파라미터를 자동 파싱
- 매물 카드 그리드 (이미지 + 제목 + 가격 + 위치 + 면적 + 구조)
- 카드 클릭으로 분석 대상 다중 선택 (체크 마크 표시)
- 검색/정렬 필터 (지역·제목·주소 검색, 가격/면적 정렬)
- "선택 매물 분석하기" → 예약율 분석 탭으로 이동

#### 📊 탭 2: 예약율 분석

- 비로그인 시: 로그인 안내 배너
- 로그인 시:
  - 분석 기간 선택 (~ 종료 월/년)
  - `disable 포함` 토글 (status=disable을 예약된 날로 집계할지)
  - "예약율 분석 시작" → 백엔드에 매물별 월간 캘린더 수집 요청

### 결과 표시 — 표 형식

| 컬럼 | 내용 |
|---|---|
| 매물 | 제목 + 위치 + 가격, 호버 시 이미지 미리보기, 클릭 시 라이트박스 |
| 주당 / 관리 | 주당 임대료 + 관리비 |
| 전체 예약율 | 연말까지 평균 예약율 (색상 강조) |
| 월별 예약율 | 동적 월 컬럼 (5월·6월·...·12월), 색상 + 호버 시 `n/N일` 툴팁 |
| 예상 월매출 | 시뮬레이션 가정 점유율 기준 |
| 적정 월세 | 추천 월세 (수익 불가 시 "불가" 표시) |
| 수익률 | 양수=초록 / 음수=빨강 |

#### 색상 임계값

| 예약율 | 클래스 | 색상 |
|---|---|---|
| ≥ 70% | `rate-high` | 빨강 (#e74c3c) |
| ≥ 40% | `rate-med` | 주황 (#e67e22) |
| < 40% | `rate-low` | 초록 (#27ae60) |

### 이미지 인터랙션

- **호버**: 단일 전역 `#previewPopup` 요소를 `position: fixed`로 커서 위치를 따라가며 표시 (300×200, 뷰포트 경계 자동 조정)
- **제목 클릭**: 라이트박스 모달 열기 (큰 이미지 + 매물명 + 위치/가격 + 상세페이지 링크)
- **`↗` 아이콘**: 새 탭으로 상세 페이지 직접 열기 (라이트박스 우회)
- **닫기**: 외부 클릭, ✕ 버튼, ESC 키

### 💡 적정 월세 시뮬레이션

분석 결과 위에 표시되는 패널. 슬라이더 조정 시 표의 `예상 월매출 / 적정 월세 / 수익률` 셀만 즉시 재계산.

#### 입력 파라미터

| 파라미터 | 기본값 | 설명 |
|---|---|---|
| 목표 수익률 | 25% | 임차인 입장에서 원하는 마진율 |
| 가정 점유율 | 85% | 시장 평균 가정치 |
| 월 운영비 | 200,000원 | 청소·관리·세금 등 월 고정비 |

#### 계산식 (`calcSim`)

```javascript
monthlyRevenue  = (weeklyRent + weeklyMgmt) * 4.34 * (occupancy / 100)
recommendedRent = monthlyRevenue / (1 + targetYield/100) - operatingCost
profit          = monthlyRevenue - recommendedRent - operatingCost
profitRate      = profit / (recommendedRent + operatingCost) * 100
```

> 4.34 = 평균 한 달의 주(週) 수 (52주 ÷ 12개월).

---

## 🔐 로그인 메커니즘

1. 클라이언트가 `/api/login`에 이메일/비밀번호 POST
2. 서버는 Selenium Chrome (헤드리스)을 띄워 33m2 로그인 페이지 자동 입력
3. 로그인 성공 시 `driver.get_cookies()`로 인증 쿠키 추출
4. 쿠키를 Flask `session["cookies"]`에 저장 (`app.secret_key`로 암호화)
5. 이후 `/api/booking-analysis`는 이 쿠키를 `requests`에 전달하여 인증 API 호출

> 쿠키 만료 시 401/403 → 클라이언트에 "다시 로그인" 안내.

---

## 🐛 디버그

브라우저에서 직접:
```
http://127.0.0.1:5000/api/debug-schedule/102555?year=2026&month=6
```

응답:
```json
{
  "raw_api_response": { ... },
  "parsed":           { ... },
  "parse_error":      null
}
```

처음 매물의 첫 달은 `monthly[ym_key]._raw_sample`에 원본 응답이 보존됨 (이후 달은 응답 크기 절감을 위해 제거).

---

## ⚠️ 주의사항

- 33m2는 비공개 API이므로 응답 스펙이 변경되면 `fetch_schedules`의 status 분류·구조 파싱 로직을 수정해야 함
- Selenium 로그인은 매 요청 ~10초 소요 (헤드리스 Chrome 부팅 + 페이지 로딩)
- Flask 개발 서버 (`debug=True`)는 production용이 아님 → 배포 시 gunicorn/waitress 등 사용
- 매물 분석은 매물당 (분석 기간 개월수 × 약 0.5초)이 걸리므로 다수 매물 선택 시 1분 이상 소요 가능
